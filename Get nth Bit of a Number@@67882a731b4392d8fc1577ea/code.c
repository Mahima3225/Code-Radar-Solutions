#include<stdio.h>
int main()
{
    int num,position=0,value;
    scanf("%d",&num);
    while(num>1)
    {  
       if(value&&num%2==1)
       printf("%d",value);
       postion++;
       else
       printf("%d",value);
    }
}