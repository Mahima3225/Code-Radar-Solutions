#include<stdio.h>
int main()
{
    int num,position;
    scanf("%d",&num);
    while(num>1)
    {
        num>>1;
        position++;
    } 
    return postion;
    if(postion==num%2==1)
    printf("SET");
    else
    printf("Not Set");
}