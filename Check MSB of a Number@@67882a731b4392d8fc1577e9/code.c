#include<stdio.h>
int main()
{
    int num,position=0;
    scanf("%d",&num);
    while(num>1)
    {
        num>>1;
        position++;
    } 
    return postion;
    if(postion==0)
    printf("SET");
    else
    printf("Not Set");
}