#include<stdio.h>
int main()
{
    int num,position;
    scanf("%d",&num);
    while(num>1)
    {
        num>>1;
        position++;
    } 
    if(return position)
    printf("SET");
    else
    printf("Not Set");
}